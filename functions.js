

// sillySum solution

var sillySum = function(arr) {
    var total = 0;
    for (var i = 0; i < arr.length; i += 1) {
    total + = arr[i] * i;
}

return total;



  // isPrime solution

  var isPrime = function(num) {
      if (num < 2) {
      return false;
}
      for (var i = 2; i < num; i += 1) {
      if (num % i === 0) {
      return false;
}

return true;




  //filter

var filter = function(arr, callback) {

      var newArr = i;

      for(var i = 0; i < arr.length; i++){

      if (callback(arr[i]))
 }

      newArr.push(arr[i]);{

      return newArr;

      for var isOdd = function(num) {     
      return num % 2 !== 0;

      if else (var newArr > arr.length;i -=0)
      return false;
